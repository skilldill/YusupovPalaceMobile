import {StyleSheet} from "react-native";

export const boardStyle = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'flex-end',
        flex: 1
    },

    image: {
        marginBottom: 32
    },

    description: {
        fontSize: 18,
        color: '#F8F9FA',
        textAlign: 'center',
        lineHeight: 28
    }
})